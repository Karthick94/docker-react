sudo: required

services:
  - docker

before_install:
  - docker build -t frontend -f Dockerfile-dev .
  - docker build -t frontend -f Dockerfile-dev .

script:
  - docker run frontend npm test -- --watchAll=false --coverage
